# Server environment variables (do NOT commit real secrets)
# Copy this file to .env and fill in real values

# Hasura GraphQL endpoint
HASURA_URL=https://hasura-e49755481ebe.hosted.ghaymah.systems/v1/graphql

# Hasura Admin Secret (use only in trusted server-side code)
# Replace with your real secret locally; do not commit it
HASURA_ADMIN_SECRET=supersecret123

# Optional: JWT if Hasura is in JWT-only mode
# If required, put a valid token here (server will send Authorization header)
HASURA_JWT=REPLACE_ME_OPTIONAL

# App port (optional)
# PORT=5000
